services:
  - type: static
    name: senior-care-landing-page
    buildCommand: npm install && npm run build
    staticPublishPath: dist/senior-care-landing-page
    routes:
      - src: /.*
        dest: /index.html
